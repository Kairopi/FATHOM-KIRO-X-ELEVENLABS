[phases.setup]
nixPkgs = ["nodejs-18_x", "python3"]

[phases.install]
cmds = [
  "npm ci --include=dev",
  "cd client && npm ci --include=dev",
  "cd server && npm ci --include=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
