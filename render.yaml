services:
  - type: web
    name: fathom
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: ELEVENLABS_API_KEY
        sync: false
      - key: DASHSCOPE_API_KEY
        sync: false
      - key: PORT
        value: "10000"
