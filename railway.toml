[build]
builder = "NIXPACKS"
buildCommand = "npm ci && cd client && npm ci && cd ../server && npm ci && cd .. && npm run build --prefix client"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
